# import tkinter as tk

# ventana =tk.Tk()
# ventana.title("ventana principal")
# ventana.geometry("1080x620")

# F = tk.Frame(ventana,bg="#f00")
# F.place(relx=0,rely=0 ,relwidth=0.25,relheight=1)

# F1 = tk.Frame(ventana, bg="blue")
# F1.place(relx=0.25, rely=0,relwidth=0.75,relheight=0.25)

# B = tk.Button(F, text="CICLO BASICO")
# B.place(relx=0.25, rely=0.35,relwidth=0.40,relheight=0.06)

# B1 = tk.Button(F, text="CICLO SUPERIOR")
# B1.place(relx=0.25, rely=0.45,relwidth=0.40,relheight=0.06)

# B2 = tk.Button(F, text="EGRESADOS")
# B2.place(relx=0.25, rely=0.55,relwidth=0.40,relheight=0.06)

# ventana.mainloop()